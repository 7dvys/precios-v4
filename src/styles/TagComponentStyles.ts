import { CSSProperties } from "react";

export const tagComponentStyles:CSSProperties = {
    padding:'0.5rem',
    flexGrow:0,
    flexShrink:0,
    border:'1px solid var(--grey-beige-0)',
    borderRadius:2,
}