import { CSSProperties } from "react";

export const sheetLabelStyles:CSSProperties = {
    padding:'0.4rem',
    flexGrow:0,
    flexShrink:0,
    border:'1px solid var(--grey-beige-0)',
    borderRadius:2,
}